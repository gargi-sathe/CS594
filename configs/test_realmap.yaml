run_config:
  costs:
    driver_hourly: 20.0
    order_op_cost: 2.5
    warehouse_base: 100.0
  entities:
    num_drivers: 3
    num_warehouses: 2
  eval:
    buckets:
      MockPlace: 15.0
    model_path: DOES_NOT_EXIST.zip
    output_dir: results/test_realmap
    scenarios_per_bucket: 1
  map:
    candidate_generation_mode: random_subsample
    candidate_subsample_size: 4
    demand_distribution: hotspot
    osmnx_place: MockPlace
    type: osmnx
  mode: synthetic
  outputs:
    output_dir: results/test
  parameters:
    delivery_target_mins: 10.0
    order_lambda: 15.0
    pick_pack_time_mins: 2.0
  policies:
    dispatch: baseline
    staging: baseline
  reward_weights:
    weight_on_time: 2.0
  run_horizon_mins: 30
  seed: 42
  training: {}
